# Basis Hawk VPS 实盘套利系统重构计划

> 本文件是项目权威文档入口与实施路线。最后更新：2026-07-29。

## 实施状态

- 已完成：通用多账户、多腿 v2 存储基础。新增不可变的 2–16 腿任务输入模型、任务/腿/执行批次/
  订单尝试/成交、套利策略/策略腿/PnL 事件和 ADL 快照表；数据库同时为每所同环境多个具名账户、
  默认交易账户、默认扫描账户、能力矩阵和账户费率预留字段。升级迁移只接受全部旧仓位、挂单和
  可执行意图均已终结的数据库，否则在变更 schema 前安全失败；通过检查后把旧意图、腿、成交、
  已关闭配对仓位和 PnL 复制为只读的 v2 历史。现有旧 API 尚未完全移入“系统管理”，但 v2
  任务控制面、纸面执行器及实盘/沙盒 Maker 追价状态机均已读写新表。
- 已完成：计划固化；PostgreSQL/Alembic、Compose/Caddy、管理员密码+TOTP、Cookie/CSRF 会话和
  AES-GCM 凭据加密与掩码配置 API；Binance、OKX、MEXC、Bybit、Bitget、Gate 六所公共行情适配。
- 已完成：管理员登录会话默认有效期统一为 7 天；数据库会话到期时间与 Secure 会话/CSRF Cookie
  的 604800 秒生命周期使用同一配置，新登录会话不会出现浏览器 Cookie 与服务端有效期不一致。
- 已完成：账户设置安全控制台。已登录管理员必须再次提供当前密码和 TOTP 才能修改密码或轮换 TOTP；
  两种变更都在同一数据库事务中撤销该管理员的全部会话并写脱敏审计。新 TOTP provisioning URI
  只在轮换响应中显示一次，浏览器保存后必须重新登录。
- 已完成：每所资金费历史预热队列按预计 30 天净收益从高到低处理；已有部分历史时使用实际计算值，
  尚无历史时用当前资金费率按相同持有期和双边手续费临时估算，只改变回补顺序而不放宽实盘健康门槛。
- 已完成：市场总览按交易所实时展示资金费历史预热比例、已完成/总标的数及本轮成功下载速度（标的/分）；
  单所队列每处理一个标的就更新状态，前端每 5 秒刷新进度，不再等待整轮数百个标的结束后才显示结果。
  历史补充同时检查实际覆盖跨度和最新结算的新鲜度，不会因最老记录超过 6 天而错误跳过仍未完成的标的；
  界面明确区分正在下载、上轮速度和等待下轮，避免把已结束轮次的速度显示成当前吞吐。
- 已完成：生产 SPA 静态文件回退严格限制在前端构建目录内；URL 编码父目录、绝对路径和指向目录外的
  符号链接只返回入口页，不能读取镜像中的源码、依赖清单或其他配置文件。
- 已完成：六所私有 REST 签名、USDT 余额、账户类型和可读取持仓模式的只读能力探测；Binance 永续
  权限从独立账户配置接口确认，不依赖已移除配置字段的 V3 账户响应。
- 已完成：六所双腿交易权限安全确证。OKX/Bybit/Bitget 从当前账号或 Key 权限字段确认；Gate 从主账号
  Key 清单唯一匹配当前 Key 并要求无限定交易对的 spot/futures 读写权限；MEXC 组合现货 `canTrade`
  与官方标明需要 Trading 权限的合约持仓模式查询。只读、缺权限或停用明确阻断，字段缺失保持未知。
- 已完成：Gate 经典账户与统一账户组合保证金模式并存支持。组合保证金必须由期货账户
  `margin_mode=2`、统一账户 `mode=portfolio` 和 `settings.usdt_futures=true` 三重确认；未开启
  USDT 永续时账户直接阻断并显示中文原因，不会在永续必然被拒绝的情况下先买入现货。读取共享
  USDT/总可用保证金，要求
  spot/futures/unified 三项写权限，现货使用 unified 账户、永续使用跨仓。组合保证金不调用 Gate 明确
  拒绝的逐合约杠杆接口，只允许保守的 1x 策略记账，并把组合保证金远端仓位统一归一化为 1x 跨仓；
  普通单向/双向及组合保证金订单均省略只适用于简单分仓模式的 `pos_margin_mode`，经典账户的逐仓
  模式已由发单前逐合约配置及回读确认。Gate REST 永续下单使用默认完整响应，不发送 TestNet
  不兼容的 `action_mode=ACK`。Gate TestNet 同账户并发私有下单会间歇拒绝合法永续委托，因此
  Sandbox 配对单固定先提交永续腿、成功后再提交现货腿；首腿失败时第二腿明确标记为未提交，
  Gate 实盘及其他交易所仍并发提交。Gate Sandbox 自动计划和真实提交都会先读取 TestNet 永续的
  标记价格、最大价格偏离、价格步长和一档盘口；保护价只允许向更保守方向收紧。若交易所当前价格
  保护带无法触及一档，自动策略等待而不创建意图，已确认的人工意图以 `market_unexecutable` 安全
  失败并释放平仓预留，双腿均不提交且不会暂停全局执行；已提交后的真实交易所拒绝仍按原规则暂停。
  Gate 订单价格和小数张数在签名及请求体中去除数据库定标产生
  的尾随零，避免 TestNet 以有效数字超过 12 位拒绝合法价格。按客户端订单号查询返回 Gate `404` 时
  严格识别为订单不存在，使 ACK 丢失对账可把未知腿终结为失败并触发成交核对与反向保护；下单收到
  明确、不可重试的 HTTP 4xx 拒绝时也直接标记该腿失败并暂停，并把交易所返回的标准错误标签归一化
  为脱敏订单腿失败码供账本展示，不保存响应正文；其他 HTTP 错误仍阻断。成交时间兼容 Gate TestNet
  返回的带小数毫秒/秒字符串，避免已成交保护流程因时间解析
  失败而停在账户对账阶段；真实开仓结算始终先从现货毛成交量扣除以基础币收取的手续费，再与永续
  基础币成交量比较、创建配对持仓或生成保护单，避免毛数量恰好相等时漏扣手续费并在后续平仓被余额
  不足拒绝。Gate 账户快照还会通过账户费率只读确认 GT 和点卡替代抵扣；两者均未启用时，开仓规划
  先按现货 taker 费率折减预计基础币净到账量，再把永续空仓向下取整到合约网格，避免已知基础币
  手续费必然触发一张反向补偿单。该扣费方式固化进交易意图并在发单前重新读取确认；设置变化时双腿
  均不提交，实际部分成交、费用资产或数量偏差仍由成交后补偿兜底。
  配置大于 1x、其他统一账户模式或模式/余额字段不完整时默认阻断。
- 已完成：独立 worker、PostgreSQL 单执行器锁、账户快照持久化及默认阻断的启动对账骨架。
- 已完成：六所现货/永续挂单与 USDT 永续仓位的统一只读 REST 对账模型。
- 已完成：远端挂单/仓位快照持久化、完整性标记及 worker 本地关联阻断。
- 已完成：纸面开仓交易意图、双订单腿账本、UUID 幂等及乐观锁状态机基础。
- 已完成：纸面双腿原子成交、手续费、成交记录及配对仓位开仓与重试恢复。
- 已完成：六所双向最优档行情，分别计算开仓与平仓可执行容量。
- 已完成：市场右侧详情按选中标的刷新可执行一档，正确展示现货卖一、永续买一价格及各自 USDT 容量，
  同时显示两腿取小后的可执行容量；Gate 缺失的批量现货数量通过单标的订单簿按需补足。
- 已完成：市场总览可在 LIVE 与 SANDBOX 间明确切换；LIVE 保持六所正式扫描器及 WebSocket，
  SANDBOX 分别从 Binance Demo、OKX Demo、Bybit Testnet、Bitget Demo 与 Gate TestNet 读取独立目录、
  批量行情和当前资金费，每所独立刷新、独立降级，并以 5 秒只读缓存限制远端请求。MEXC 因没有完整的
  现货与 USDT 永续沙盒而明确显示不支持，绝不回退正式网行情。测试环境历史未持久化时明确标注
  24h/7d 和净收益为当前费率回退估算，不与正式网行情或运营交易候选混用。每个标的及详情均提供
  受控的交易所永续页面外链；Binance、Bybit 与 Gate Sandbox 指向对应 Demo/Testnet 页面。
- 已完成：Gate 自动策略在规则初筛后为每轮最多 20 个候选顺序补取两腿一档，已有仓位优先以保障平仓，
  其余按策略排名补足；Sandbox 只使用 TestNet 标的目录、实际盘口和原生交易规则生成订单腿，自动补偿
  也重新读取 TestNet 规则与深度，目录、规则或盘口不可用时安全跳过，不再因批量 ticker 缺少现货数量
  或测试网与实盘步长不同而永久无候选。
- 已完成：Gate Sandbox 自动策略直接从 TestNet 标的目录、批量行情和当前资金费生成独立候选，不再要求
  与同一时刻的 Gate 实盘健康机会相交；最多 20 个候选在读取 TestNet 双腿一档时同步回看测试网资金费
  历史，历史尚不足时仅在 Sandbox 以当前资金费年化补齐缺失的 24h/7d 门槛和预计净收益，用于持续验证
  自动配对执行。实盘仍严格要求完整历史、健康质量和全部原有收益阈值，不采用该回退。
- 已完成：纸面配对平仓、reduce-only 永续腿、双腿费用及已实现净盈亏。
- 已完成：纸面开仓部分成交按共同数量建仓、多余单腿反向补偿、重启续跑及补偿失败全局暂停。
- 已完成：纸面部分平仓按共同数量递减仓位、分摊开仓费、累计净盈亏并允许后续继续平仓。
- 已完成：六所按订单查询真实成交的统一 REST 模型、费用符号和分页完整性保护。
- 已完成：六所私有账户实际资金费收支统一 REST 模型与幂等账本；worker 首次回看 24 小时并从最后
  结算时间重叠增量核对，账户状态单独报告抓取完整性和本轮记录数，运营台可审计最近实际收支。
- 已完成：远端成交按本地订单腿幂等持久化，并重算累计成交量、均价和账户成交完整性。
- 已完成：六所按客户端订单 ID 找回 ACK 丢失订单、持续刷新已关联 IOC 终态、严格核对本地订单腿并
  继续成交对账；部分成交后撤销保持终态且不被成交汇总重新标为活动。
- 已完成：六所下单精度、最小数量/名义额和合约乘数目录，以及双腿共同数量向下取整校验。
- 已完成：订单腿持久化原生下单数量及基础币乘数，为按“张”成交的永续腿提供可审计的基础币换算。
- 已完成：真实开仓计划可在发单前固化环境、1–10 倍杠杆、共同基础币数量、两腿原生数量、合约乘数、
  保护价及符合各所约束的唯一客户端订单 ID。
- 已完成：真实开平仓预览票据持久化安全边界，绑定管理员、交易参数、交易规则、配置和 60 秒有效期；
  票据同时固化双腿绝对保护价。确认时刷新 15 秒内盘口和容量，允许价格在原最大滑点边界内变化，超出
  边界、规则/配置变化、票据过期、换管理员或同票据换 UUID 幂等键均拒绝。
- 已完成：配对持仓 API 和界面以“剩余共同基础币数量 × 现货开仓成交均价”展示剩余 USDT 名义额，
  并从不可变开仓意图返回实际请求且已由下单预检确认的永续杠杆；基础币原始数量仍保留在 API 中供
  精确平仓、对账和审计使用。
- 已完成：真实意图先在 PostgreSQL flush 父记录，再写入双订单腿和提交事务，避免无 ORM relationship
  排序提示时子腿抢先触发外键失败；SQLite 测试统一启用外键，并由容器验收直接覆盖 PostgreSQL 写入顺序。
- 已完成：`sandbox`/`live` 开仓预览与确认 HTTP 入口；预览展示双腿参考价/保护价、原生数量、合约乘数、
  预计费用、保证金需求和最坏基差，确认仅持久化 `planned` 意图，由唯一 worker 发单。
- 已完成：`sandbox`/`live` 平仓预览与确认 HTTP 入口；票据绑定既有配对仓位和管理员，按开仓时的
  合约乘数校验当前规则，展示 reduce-only 双腿保护价、数量、预计费用和盈亏；确认仅持久化平仓意图。
- 已完成：交易账本以交易意图编号关联父意图、订单腿与成交；父意图按自身或任一订单腿的最近活动排序，
  并同时展示创建时间和最近活动时间，避免远端成交延迟补记后子腿浮到顶部而父意图看似缺失。
- 已完成：真实平仓执行与结算闭环；worker 发单前按账户快照精确匹配全部本地/远端逐仓或跨仓空仓，只提交现货卖出与
  永续 reduce-only 买回，每轮最多提交一个意图；终态成交等量时按真实均价、乘数和手续费关闭或递减
  配对仓位，双零成交安全失败，失衡或费用无法折算时全局暂停。
- 已完成：不可变自动策略版本和独立自动状态控制；完整配置覆盖环境/交易所、名义额、杠杆、敞口、
  并发、费率/收益/基差、盘口、滑点、日亏、重入、持有、清算缓冲及平仓阈值。默认 disabled，只有
  全局 ready、所有目标凭据存在且管理员显式确认后可启用，暂停/恢复/禁用均审计。
- 已完成：API 行情进程每次 5 秒价格刷新后按交易所聚合覆盖持久化最新完整机会，唯一 worker 可从
  PostgreSQL 读取同一行情时间、质量、双向盘口容量和当前交易规则，不依赖另一个进程的内存或分钟历史。
- 已完成：VPS 存储增长治理。趋势历史改为每小时采样并硬限制最多保留 7 天，升级时把既有分钟快照
  降采样后压缩；约 3,000 条实时机会按交易所聚合为 6 行 JSON，并为主表及 TOAST 设置积极 autovacuum，
  保持 5 秒行情和 worker 决策不变。成功部署后把 Docker build cache 压到 1 GB 并清理悬空镜像，把
  systemd journal 限制为 200 MB/7 天且至少保留 1 GB 空间；备份容器重启会从最近归档续算周期，
  避免升级前安全备份之后立即再生成一份重复归档。
- 已完成：每次纸面或真实平仓按唯一平仓意图写入独立已实现 PnL 事件，记录数量、毛盈亏、开仓费分摊、
  平仓费和净盈亏；重复结算不会重复记账，自动策略可按环境、交易所和 UTC 日界准确执行每日止损。
- 已完成：确定性的自动策略评估器每轮至多选择一个动作并优先平仓；开仓联合检查三档 APR、净收益、
  最低/最大开仓基差、最小成交额、盘口容量、每日亏损、并发、单所/全局敞口、单标的唯一仓位及重入间隔，并按
  “单笔上限、盘口容量除以安全倍数、单所剩余额度、全局剩余额度”取小动态确定名义额；平仓按
  清算缓冲、止损、最长持有、资金费、净收益、平仓基差和止盈分级，并拒绝陈旧或非健康行情。
- 已完成：自动策略新增最低开仓基差，与最大开仓基差共同形成闭区间；低于下限的永续折价和高于上限
  的异常溢价均禁止开仓。新建策略默认下限为 0；升级前不可变策略缺少该字段时使用接近 -100% 的
  兼容下限保持原版本语义，管理员必须保存并启用新版本才会采用新的安全边界。
- 已完成：自动评估器接入唯一 worker 的完整账户对账末尾，只有本轮重新确认全局 `ready` 才按生效策略
  生成一条可审计真实意图并立即唤醒下一轮执行；行情时间、策略、动作和标的生成确定性幂等键，重启或
  重复事件不重复规划。所有超过 15 秒仍未提交的真实开平仓意图原子失败，过期平仓同时释放仓位预留。
- 已完成：带普通确认的全局执行暂停/恢复请求；暂停先持久化阻断新提交，worker 对六所全部远端活动
  订单逐笔撤销并保持 `paused`，撤单 ACK 只表示已受理，后续仍由订单/成交对账确认终态；恢复只进入
  `reconciling`，必须重新通过账户、订单、成交、仓位和私有流检查才回到 `ready`。
- 已完成：独立标记的紧急配对清仓预览/确认；允许最高 25% 的紧急保护滑点及资金费历史降级机会，但
  仍要求 15 秒内的双向价格、完整交易规则和足够平仓盘口。确认后全局保持暂停，执行器只放行该紧急
  平仓，仍严格提交现货卖出与永续 reduce-only 买回，不开放单腿或任意订单。
- 已完成：真实开仓和平仓主腿终态数量失衡时，事务内按多余基础币数量创建唯一反向补偿腿并全局暂停；
  worker 使用 15 秒内最新双向盘口、当前交易规则及策略紧急滑点提交保护性 IOC。补偿完整成交后仅按
  主腿共同数量建仓/减仓，补偿往返价差与全部真实费用计入成本或 PnL；ACK 不确定只查单，部分或零补偿
  保持 `manual_review`；任一已成交主腿缺失可靠均价时同样安全转入人工复核而不尝试结算，成功后也必须
  由管理员重新对账恢复。
- 已完成：保护性补偿单在持久化提交数量前按当前交易所数量步长量化；买入保护向上取整、卖出保护向下
  取整，使不可成交尾差只成为小于一个步长的现货尘埃，不留下额外永续裸敞口。订单腿同时保存补偿目标
  基础币数量和允许的单步尾差，结算只在补偿完整成交、尾差方向安全且严格小于该界限时继续。账户对账
  异常使用脱敏的阶段/错误代码，前端显示中文具体原因；机会 WebSocket 在浏览器并发关闭后安静退出，
  不再产生 `websocket.send` after close 的 ASGI 异常。
- 已完成：真实开仓执行器的安全提交基础：仅在全局 `ready` 时预检余额、权限、持仓及保证金模式；
  已有远端永续仓位必须按交易所原生数量、方向、杠杆和逐仓/跨仓模式与全部本地配对仓位精确一致，
  一致时允许继续建立不同标的配对仓位，额外、缺失或冲突敞口仍阻断。确认逐仓或组合保证金跨仓杠杆后
  原子标记双腿 `submitted`、并行发单并独立持久化 ACK；任何不确定 ACK 全局暂停且不重发；
  常驻 worker 每轮对账前执行已确认的真实计划，并在同一轮立即核对远端结果。
- 已完成：真实订单预检失败按受数据库约束的安全代码区分凭据、账户快照、远端状态、权限、持仓模式、
  未匹配订单/仓位、余额、永续保证金/杠杆配置和平仓状态不一致；意图阻断代码与全局暂停在同一事务
  持久化，执行页和账本显示中文原因，但不保存交易所响应、签名 URL 或异常原文。
- 已完成：API 与 worker 分进程部署下的手动意图低延迟调度。worker 在 60 秒完整对账间隔内每秒只查询
  PostgreSQL 是否存在当前执行状态允许消费的真实 `planned` 意图，发现后立即进入既有预检、发单及
  完整对账；普通暂停不会因未执行开仓形成忙循环，紧急平仓仍可在暂停状态被及时发现，15 秒行情安全线不放宽。
- 已完成：真实开仓在两腿 IOC 终结且成交分页完整后按合约乘数比较基础币成交量；等量成交创建配对仓位，
  双零成交失败，失衡或无法折算的手续费进入人工复核并保持全局暂停。
- 已完成：启动对账按订单双 ID 和完整语义匹配远端挂单，并按本地配对仓位、永续原生数量、方向、
  当前账户要求的逐仓/跨仓模式及杠杆精确匹配远端仓位；未知或冲突敞口继续阻断。
- 已完成：私有订单、成交和仓位事件流的持久化健康状态、30 秒心跳新鲜度、进程重启失效及断线自动暂停
  安全边界；认证连接按交易所逐步接入。
- 已完成：私有流通用监督器，连接成功后登记订阅、仅以真实事件或已验证 ping/pong 更新心跳，并在异常
  断线后按上限 30 秒指数退避重连。
- 已完成：Binance 现货签名 WebSocket API 与 USDT 永续 listenKey 私有流连接、双连接 ping/pong、
  永续 listenKey 续期和安全关闭适配，并已装配到常驻 worker。Sandbox 统一使用同一组 Binance
  Demo Trading Key 对应的 `demo-api`、`demo-ws-api`、`demo-fapi` 和 `demo-fstream` 官方地址，
  不再把 Demo Key 错发到凭据隔离的 Spot Testnet。
- 已完成：OKX 生产/模拟盘私有 WebSocket 登录、通用订单（含成交更新）、仓位和账户频道订阅、文本
  ping/pong 保活及 worker 装配；专用 fills 频道因 VIP 限制不作为就绪条件。
- 已完成：Bybit 生产/测试网私有 WebSocket 认证、全品类订单、独立成交、仓位和钱包频道订阅、应用层
  ping/pong 保活及 worker 装配。
- 已完成：Bitget 连接前只读识别 Classic V2/UTA V3，按账户代际选择生产/模拟盘私有域名和订单、成交、
  仓位、账户频道，文本 ping/pong 保活并装配 worker；识别不清时不连接且不跨代回退。
- 已完成：Gate LIVE/SANDBOX 现货订单/成交与 USDT 永续订单/成交/仓位双私有连接，按环境选择官方
  REST 与 WebSocket 地址，订阅前只读获取永续用户 ID，永续数量使用十进制协议头；整张在 JSON 中
  发送整数，只有小数张才发送字符串，兼容 `enable_decimal=false` 合约。双连接均通过协议 ping/pong
  验证并装配 worker；两套凭据、连接、对账和交易意图按环境完全隔离且不得跨环境拼腿。
- 已完成：MEXC LIVE 现货 listenKey 私有订单/成交/账户 Protobuf 订阅与合约签名登录私有流，现货
  listenKey 每 30 分钟续期并在关闭时释放，两条连接均使用官方应用层 ping/pong 并装配 worker；
  沙盒明确拒绝且不回退实盘。
- 已完成：六所任一私有订单、成交、仓位或账户事件会唤醒同一 worker 的严格 REST 对账循环；突发事件
  合并后串行执行，按现有双 ID、分页完整性和幂等规则更新订单、成交及仓位账本，60 秒周期对账继续作为
  断线与漏事件恢复路径。
- 已完成：交易所凭据热重载。保存、替换、删除或更新 Bybit 模式声明会先把非暂停执行状态切到
  `reconciling`；常驻 worker 每秒比对交易所、环境与凭据更新时间，自动启动、替换或停止对应私有流，
  连接状态变化立即唤醒严格 REST 对账，不再要求重启 worker，且不会清除既有人工或安全暂停。
- 已完成：通知 outbox 按事件去重键和通道独立持久化，支持 PostgreSQL 跳锁并发认领、进程崩溃后的
  超时回收、最多 8 次指数退避以及 sent/dead 终态；只保存归一化错误码，不保存可能含凭据的远端异常。
- 已完成：worker 独立投递 Telegram Bot API 与通用 SMTP STARTTLS/SMTPS；两通道独立结算，
  Telegram 使用受保护纯文本，邮件在独立线程执行 TLS 发送，认证、网络、限流和远端错误只映射为
  脱敏错误码；SMTP 协议/TLS/4xx/5xx 错误在通用网络异常之前分类，避免全部误记为传输故障。通道未
  配置时不影响交易循环。
- 已完成：通知投影器持久跟踪全局执行、逐账户对账及交易意图状态指纹；成交完成只发 Telegram，
  成交失衡、失败、人工复核、全局暂停和账户异常同时发 Telegram/邮件。相同故障持续期间不重复刷屏，
  恢复后再次故障会生成新一代告警；worker 启动只建立基线，不回放历史告警。
- 已完成：Telegram webhook 使用官方 secret header 和管理员 chat ID 双重白名单，限制 32 KiB 请求并
  以 update ID 去重；仅实现 `/status`、`/positions`、`/alerts`、`/health` 查询，未知命令只返回帮助，
  不存在交易、暂停、恢复或配置命令。回复仍经通知 outbox 异步发送。
- 已完成：每个 UTC 日界生成前一完整自然日汇总，同时进入 Telegram 和邮件；包含真实已实现净盈亏及
  事件数、开平仓/失败数、当前活动配对仓位和异常账户数。worker 启动建立日期基线，不补发旧日报，
  同一日期依靠持久化 generation 只发送一次。
- 已完成：内部划转持久化账本及默认关闭的全局安全限额；资产固定 USDT、方向固定现货↔USDT 永续，
  UUID 幂等与请求指纹防止重放变参。环境变量只在首次访问时初始化限额，之后管理员可在内部划转页
  显式确认并持久化修改单次和 UTC 日累计限额；限额设置行与计划划转在同一事务中锁定，更新写入脱敏
  审计。两项同时为 0 表示禁用，计划划转仍立即暂停新交易，绝不提供提现、跨账户或跨交易所字段。
- 已完成：Binance、Bitget Classic、Gate、MEXC LIVE 的 USDT 现货↔USDT 永续划转私有适配；
  Binance、Bitget Classic 和 MEXC 使用官方查询接口确认远端状态，Bitget 使用客户端划转 ID 防重。
  Gate 成功提交返回的 `tx_id` 即远端完成凭据，随后只用余额确认到账。OKX、Bybit 与 Bitget UTA
  当前交易账户共享现货和永续余额，明确识别为无需划转，不模拟不存在的余额移动。
- 已完成：内部划转接入唯一 worker 与管理员 API。提交前持久化来源、目标和预期目标余额并保持全局
  暂停；每轮最多处理一笔，ACK 不确定时仅 Bitget 可按客户端 ID 安全找回，其他所转人工复核且
  绝不盲目重发。Gate 成功提交后在同一轮刷新余额；所有所都需新快照达到预期目标余额才完成，
  15 分钟超时转人工复核。HTTP 写入口
  要求 `confirmed=true`、UUID 幂等键、全局 ready、已配置凭据、非共享余额及非零环境限额。
- 已完成：Web 运营控制台第一阶段接入真实后端：全局执行与逐账户对账状态、暂停/重新对账（阻断状态
  也可直接请求全量刷新，仅正在对账时禁用）、加密凭据
  保存/删除和按需余额探测、配对持仓、内部划转及自动策略启停。所有危险动作在界面再次普通确认，
  实盘凭据不回显；全局状态为 `reconciling` 时前端每 3 秒刷新执行与逐账户状态，进入任一终态后
  自动停止轮询；浏览器已验证各页布局、真实 API 加载和无控制台错误。
- 已完成：内部划转页随交易所和环境选择按需读取账户快照，提交前同时展示现货可用、永续可用与权益、
  独立/共享余额结构、账户模式和读取时间；当前划出账户醒目标记，可手动刷新，切换期间不会沿用上一
  账户的旧值，单个快照错误也不会阻断划转记录页的其他功能。页面同时显示并编辑全局单次和 UTC 日
  累计限额，保存前要求普通确认，禁用状态会直接阻止提交按钮。
- 已完成：Gate 现货与 USDT 永续账户划转不再误用仅适用于主子账户划转的
  `/wallet/order_status`。成功的 `/wallet/transfers` 响应及 `tx_id` 作为远端完成凭据，同一 worker
  轮次立即刷新余额确认到账；提交 ACK 不确定时转人工复核且绝不重发。Binance、Bitget Classic、
  Gate、MEXC 的划转提交与查询路径已按各所官方文档重新核对。
- 已完成：Web v2 工作区按参考项目的信息架构重构为全浅色固定侧栏、窄状态栏和高密度数据主区。
  API 密钥、自动下单、资金统计、持仓总览、套利机会、总览看板、策略列表、成交选择、预警、ADL、
  邮件和账户设置均为一级页面；旧执行、交易、划转、自动策略、审计与运维页收入口“系统管理”。
  窄屏自动收拢为图标栏，重构不放宽任何确认、认证、对账或实盘安全边界。
- 已完成：全站辅助文字、字段标签、表头、状态胶囊和侧栏元信息的显式字号下限统一提升到 11px，
  保留当前桌面信息密度的同时避免策略配置等高密度页面出现 7–10px 难读文字。
- 已完成：Web 手动真实配对交易接入 60 秒开仓/普通平仓/紧急平仓预览与明确确认。界面展示双腿
  保护价和原生数量、余额/保证金、费用、基差及预计平仓盈亏，实盘票据醒目标记；确认只持久化 UUID
  幂等意图，仍由唯一 worker 预检并发单。
- 已完成：手动开仓金额不足时按当前报价、共同数量网格及两腿最小交易规则计算动态最低 USDT 金额，
  API 返回结构化错误，前端显示可直接操作的中文建议值，不再暴露无上下文的英文步长错误。
- 已完成：手动开仓金额超过实时双腿一档容量时返回结构化最大金额并显示中文重试建议；凭据缺失、
  盘口不可用、机会失效、规则缺失和行情陈旧等常见预览错误同步使用中文提示。
- 已完成：手动开平仓确认时重新读取 15 秒内盘口、容量和交易规则；报价在原预览绝对保护价内可继续
  确认，且订单仍使用原保护价，超出原最大滑点、规则/配置变化或 60 秒票据过期时显示中文说明并移除
  失效票据。订单簿采集时间刷新本身不再误判为交易条件变化。
- 已完成：VPS PostgreSQL 自动备份与受控恢复。独立非 root Compose 服务每日生成 AES-256-GCM
  认证加密的 custom archive 和 SHA-256 校验文件，保留 7 个日备份及 4 个周备份；验证全程不落地
  明文，完整认证后静默解析 archive TOC，并正确接受 `pg_restore --list` 成功后的提前关闭输入；恢复
  默认拒绝非空数据库，覆盖恢复必须同时显式提供确认与清理参数；镜像内运行时代码显式归属固定非 root
  UID，不依赖部署 checkout 的文件 umask。
- 已完成：Web 自动策略完整编辑器覆盖后端全部环境、六所选择、资金/敞口/并发、三档 APR、净收益、
  基差、盘口、滑点、日亏、重入、持有、清算缓冲及平仓阈值。保存只创建不可变新版本且不启用；
  `disabled` 明确启用最新版本，`paused` 只恢复原生效版本，全局非 ready 时两者都禁用。
- 已完成：自动策略启用/恢复遇到全局执行状态并发变化时，前端立即重新读取服务端真相源，不再保留
  过期的“执行就绪”按钮状态；拒绝原因、当前执行状态及内部划转/对账原因使用可操作的中文说明。
- 已完成：管理员审计与通知投递历史 API/前端。两个只读入口支持受限分页和精确过滤；审计详情在服务端
  递归屏蔽密码、Token、签名、密文、nonce 和 API Key 等敏感键，通知历史只返回投递元数据、状态和
  脱敏错误码，明确不返回正文及去重键。浏览器已验证布局、真实 API 加载和无控制台错误。
- 已完成：全局交易账本 API/前端。管理员可浏览最近 100 条交易意图、订单腿、成交和已实现 PnL；
  新产生的失败意图持久化预定义脱敏失败代码并在页面显示中文原因；升级时可由“两腿均未提交”可靠判断
  的旧真实失败回填为行情过期，其他无法追溯的记录明确标注未记录；
  后端查询上限 500，意图/订单支持精确状态过滤，所有金融值继续以十进制字符串传输，界面分别展示
  毛盈亏、开仓费、平仓费和净盈亏。意图、订单腿和成交表统一展示同一意图短编号，订单腿同时区分
  创建时间与真实状态更新时间；远端订单和成交对账只有在关联 ID、成交汇总或状态实际变化时才更新
  订单腿时间，无变化的周期核对不会把历史订单误排成新订单。
- 已完成：配对持仓页每 5 秒刷新未实现价格盈亏。系统以当前现货买一作为卖出估值、永续卖一作为
  空单回补估值，展示两腿平仓估值与行情时间；结果不混入资金费、开仓费或预计平仓费，行情超过
  15 秒或 Gate TestNet 公共行情不可用时返回空值而不沿用陈旧价格。
- 已完成：配对持仓页同时显示按当前一档立即平仓的预估最终收益；在未实现价格盈亏基础上合并既有
  已实现净 PnL 和仓位存续期间已入账的实际资金费，再扣除剩余开仓费及按不可变开仓费率估算的双腿
  平仓费。现货与永续开仓/平仓估值、费用和资金费移入每行可展开二级详情，主表只保留核心仓位与收益
  指标；行情不可用时不生成最终收益估值，也不承诺未来资金费或实际成交结果。
- 已完成：运维通知测试与备份状态。管理员显式确认后可把独立测试事件送入已配置 Telegram 或邮件
  outbox；未配置通道明确拒绝，不绕过 worker 的重试与脱敏。API 通过备份卷展示归档数量、最近
  文件时间/大小及校验文件是否存在，不读取备份密钥或解密归档；前端执行页与通知页提供对应视图。
- 已完成：受控运维清理。前端列出全部加密归档，可选择最多 100 个旧归档批量删除；服务端先验证整批
  文件名、存在性和非最新约束，再在一次明确确认后删除归档及校验文件，单个删除仍可用。通知日志只
  清理超过指定保留期的 `sent/dead` 终态，待发送/重试记录和不可修改的管理员审计不受影响。全部
  Compose 服务的 Docker JSON 日志限制为 10 MiB × 5 文件，不向 Web 容器暴露 Docker Socket。
- 已完成：前端受控软件更新。API 只能向宿主机 systemd 代理提交检查或更新到已锁定 HTTPS Git
  远端/分支最新提交的固定请求，不持有 Docker Socket、Git checkout 或任意宿主机命令权限；API 状态
  检查和请求入队一致拒绝符号链接请求目录，代理拒绝脏工作区、远端/分支变化、非快进和检查后远端漂移。
  确认更新必须在数据库行锁下从全局 `ready` 原子切换为专属软件更新暂停；同一目标部署失败后只允许
  从该专属暂停重试，人工暂停、对账中或成交失衡等安全状态不能被更新入口覆盖。随后由既有部署流程
  创建加密备份、快进，并从同一 checkout 重建 API、worker、backup 三个本地镜像后再迁移和健康检查，
  避免 Web 与交易执行器运行不同版本；新 worker 启动前，代理只把自己创建的“软件更新请求”暂停
  原子切换为 `reconciling`，随后自动完成账户、订单、成交、仓位和私有流全量对账。人工暂停及任何
  成交失衡、补偿失败等安全暂停都不能由更新流程清除；状态与脱敏错误码回传前端。已有部署停止应用
  服务后的任一步骤失败时，退出钩子都会尽力重启原有 API、worker 和备份容器，不能因严格后置检查
  失败而把 Caddy 永久留在 502。
- 已完成：可显式启用的 VPS 自动更新。root systemd timer 每 5 分钟检查锁定 GitHub 仓库与分支，
  仅当远端头是当前版本的快进后继且该提交的 `CI` push workflow 已明确成功时才排队；排队前必须用
  数据库行锁确认全局执行为 `ready` 并切到软件更新暂停。CI 未完成/失败、工作区有改动、非快进、
  远端变化或已有人工/安全暂停时均跳过，不向 GitHub 保存 VPS SSH 私钥，也不绕过既有备份、迁移、
  健康检查和更新后自动对账。安全暂停成功后才释放更新锁并原子发布请求文件，避免 systemd 路径
  监听器在检查过程中抢跑、争锁并触发启动频率限制。
- 已完成：六所各 500 个候选的 3,000 标的端到端负载验收。行情服务使用标的键索引重算并持久化全部
  机会，REST 单页上限及首页初始请求同步扩展到 3,000，不再出现后端已扫描但前端只取得前 300 个的
  截断；负载测试同时验证每所 500 上限、健康计算、完整持久化和越界拒绝。
- 已完成：可重复的隔离容器验收和故障注入。单命令构建非 root、只读根文件系统的 API 与备份镜像，
  在 PostgreSQL 17 实际执行全部迁移并运行模型/数据库漂移检查，验证 API 进程重启、数据库重启后的
  连接恢复、第二 worker 被
  advisory lock 拒绝、单轮 worker、认证加密备份、空库恢复及非空库拒绝覆盖；锁竞争验收会先通过
  PostgreSQL 探针确认主 worker 已实际持锁，不依赖固定睡眠猜测启动完成；临时容器、网络和卷始终
  自动清理，CI 同步执行。
- 已完成：生产 Python 依赖使用可重现锁文件安装，SQLAlchemy 显式启用异步运行所需的 `greenlet`；
  CI 对锁文件运行 `pip-audit`，存在已知安全公告的运行时版本不能通过交付检查。
- 已完成：CI 使用基于 Node 24 的 `actions/checkout@v6`、`actions/setup-python@v6`、
  `actions/setup-node@v6` 和 `pnpm/action-setup@v6`，不再依赖 GitHub 已弃用的 Node 20
  Action 运行时。
- 已完成：交易预览安全测试在用例执行时生成 60 秒票据时间，较慢 CI runner 不会再把管理员或行情
  指纹冲突测试误判为票据过期，同时仍独立覆盖真实过期拒绝分支。
- 已完成：幂等 VPS 一键部署脚本。首次部署生成权限为 600 的 `.env`、数据库密码及相互独立的凭据/
  备份密钥，可选按 Docker 官方 apt 仓库安装 Engine/Compose；默认不启用或修改 UFW，只有显式
  `--enable-ufw` 才配置防火墙。已有部署绝不覆盖配置，
  每次部署都从同一源码版本重建 API/worker/backup，升级前停止三者并生成加密备份，之后才迁移、启动
  并验证行情就绪与最新备份，不能复用旧 worker 镜像。远程 bootstrap 可在空 VPS 自动安装 Git、
  clone 到 `/opt/basis-hawk`，重复运行只允许
  origin/分支一致的干净 checkout 并执行 fast-forward 更新；管道启动时会重新连接控制终端完成部署
  确认和首次管理员创建，管理员密码会预告 12 字符下限并在长度不足或确认不一致时原地重试；真正
  无终端运行仍要求显式非交互选项。首次 TOTP URI 仅暂存在部署进程内，并在成功或失败退出时作为
  最后一段醒目输出，避免被后续容器日志淹没。
- 已完成：仅限 VPS 终端的管理员 TOTP 轮换命令。必须隐藏输入并验证当前管理员密码，成功后在同一
  数据库事务中替换加密 TOTP、注销全部现有会话并写脱敏审计，最后只输出一次新 provisioning URI；
  密码错误或并发轮换不修改 TOTP。
- 已完成：启动对账在全部已配置账户的余额、权限、模式、远端订单/成交/仓位关联和私有流均通过时进入
  `ready`；任一账户失败、阻断或心跳在最终确认前失效都保持 `blocked`，既有安全暂停不被覆盖。
- 已完成：Binance、OKX、Bybit 现货/永续保护性限价 IOC 下单、撤单、逐仓和 1–10 倍杠杆私有适配。
- 已完成：Bybit 空仓账户先读取 USDT 线性持仓，无法取得模式时再以 `BTCUSDT` 只读探测有效
  `positionIdx`；若空子账户仍不返回模式，可在不重新输入 Key 的情况下明确声明当前单向/双向设置，
  目标标的在配置杠杆前仍会再次核对自身模式。UTA 2.0 逐仓余额按币种钱包扣除订单、仓位、锁定及
  赠金占用计算，不再误用该模式不适用的账户级可用余额。
- 已完成：Bitget Classic V2 与 UTA V3 现货/永续保护性限价 IOC 下单、撤单、逐仓和
  1–10 倍杠杆私有适配；写操作前只读识别账户代际且不跨代重试。
- 已完成：Gate LIVE/SANDBOX 现货/永续保护性限价 IOC 下单、撤单、逐仓和 1–10 倍杠杆私有适配；
  TestNet 使用独立 API Key 和官方测试网端点，不复用或回退实盘凭据与地址。
- 已完成：Gate 批量 ticker 缺少现货最优档数量时，手动实盘开仓预览与确认会为所选标的即时读取
  现货和 USDT 永续一档订单簿，按合约乘数计算真实双腿容量；订单簿缺失或不足仍在下单前硬阻断。
- 已完成：MEXC 现货及特批 Standard V1 合约 IOC 下单、撤单和逐仓杠杆适配；合约写探测失败即只读。
- 已完成：v2 多账户凭据服务和 `/api/v2/accounts` 管理接口。同一交易所/环境可保存多个具名加密
  账户，通过稳定 `account_id` 独立读取密钥、余额快照和账户级 Maker/Taker 费率；交易默认与扫描
  默认各自唯一。旧 API、私有流和旧执行器在通用账户级 worker 接管前只使用交易默认账户。
- 已完成：六所通用私有订单原语。现货和永续支持任意买卖方向、单向/双向 long/short、逐仓/跨仓，
  并统一映射 Maker post-only、保护性 IOC 和原生 Market；旧双腿交易经薄兼容层继续使用保护性 IOC。
  Binance、OKX、Bybit、Bitget、Gate 可读取原生 ADL 并归一为 1–5 级且 5 为最高风险；MEXC 明确
  声明只支持 ADL 私有事件、不伪造实时等级。Gate 现货市价买入因原生接口使用计价币数量而安全拒绝
  基础币数量任务，任务可改用保护性 IOC。
- 已完成：`/api/v2/execution-tasks` 持久化任务控制面。2–16 腿任务用 UUID 幂等键原子写入父任务和
  有序腿；账户 ID 必须存在且实盘/沙盒环境严格匹配。预检按任务涉及的每个账户独立读取余额与完整
  远端交易状态，只持久化脱敏摘要并生成 60 秒有效窗口；管理员必须带最新乐观锁版本和
  `confirmed=true` 才能排队，过期、重复启动、全局交易暂停或并发版本变化均拒绝。未启动任务可取消，
  已运行任务不能用取消接口绕过补偿流程。
- 已完成：每条任务腿显式持久化六所之一的 `exchange`，纸面任务不再依赖账户或用重名 symbol 猜所。
  账户腿同时要求账户交易所与腿完全一致。`0035` 迁移依次从账户、旧意图和唯一 instrument 匹配回填；
  仍有歧义时在列改为非空前中止，避免把纸面历史错误归属到另一交易所。
- 已完成：v2 任意多腿纸面执行器接入唯一 worker。每轮领取一个 queued/running 纸面任务并持久化 run，
  从对应交易所最新健康盘口按 Maker/Taker 方向估值，永续用 instrument 合约乘数在基础币与原生张数间
  换算；全部腿的订单、成交、手续费和新策略在同一事务完成，重启可继续已领取 run。任一腿行情陈旧、
  symbol 不匹配或乘数未知时以脱敏失败码终结整次 run，不留下半套纸面策略。
- 已完成：v2 实盘/沙盒多腿可恢复执行状态机。worker 每轮只推进一个持久化动作：先按客户端 ID 查重，
  再提交 created 订单，随后用完整 REST 查单/成交更新账本。六所 Maker 价格读取对应实盘或沙盒的
  真实 REST 深度第 N 档，不再按 tick 虚构档位；只有既有委托真正掉出最新前 N 档才先持久化并发送
  撤单，盘口暂不可用时保留挂单。旧单查到 filled/canceled/rejected/failed 后才创建带 parent/chase
  序号的新订单；追价耗尽按策略回退 IOC/Market。主腿每个成交块优先计算 1–N 对冲缺口再继续主腿，
  支持实时和累计百分比触发。
  重启复用 running run 和非终态订单；失败前重新读取实际成交，远端状态不确定时进入 manual_review。
  当前自动配置只放行逐仓；跨仓任务在能够逐所确认 1–10x 杠杆前失败关闭，不会带未知杠杆下单。
- 已完成：v2 实盘多腿失败补偿与现货库存边界。每笔执行订单独立固化真实买卖方向、reduce-only
  和 primary/compensation 用途；普通腿出现确定性失败且已有终态成交时，任务进入可重启的
  compensating 阶段，逐腿按实际成交量提交反向保护 IOC，永续补偿强制 reduce-only。全部反向成交
  后任务以原失败码安全终结并保持全局暂停；补偿失败、数量越界或任何未决/未知订单转人工复核。
  所有六所的现货卖单在写接口前重新读取对应基础币可用余额，不足时不提交。
- 已完成：v2 任务活动和组合读模型。任务详情可读取按执行轮次排序的 run、订单尝试、Maker 追价父子链
  与逐笔成交；策略列表/详情返回 2–16 条有序腿的交易所、角色、账户、方向、入场价、剩余基础币数量、
  手续费、资金费和净 PnL，且不返回任何凭据或交易所原始错误。
- 已完成：v2 三类跨所机会聚合。资金费套利组合任一交易所现货多腿与任一交易所永续空腿；跨所资金费
  套利组合高 funding 永续空腿与低 funding 永续多腿；基差交易按入场价差、持有天数 funding 和
  双边往返费率后的预计收益排序。候选只使用新鲜健康行情，投影默认实盘交易账户及其 actual/manual
  Taker 费率，缺失单项才回退扫描器默认费率。
- 已完成：v2 Web 控制面接入跨所机会、任务、策略和成交读模型。机会可一键带入 2 腿任务草稿，
  任务编辑器可继续增加到 16 腿；每腿独立配置交易所、具名账户、现货/永续、买卖方向、Maker/
  保护 IOC/Market、盘口档位、最多 50 次追价、回退方式及逐仓/跨仓 1–10x。任务列表通过预检、
  最新版本确认启动和未运行停止接口操作；策略、持仓、资金统计及成交选择器读取 v2 持久化账本。
  1440px 真实浏览器验收覆盖机会表和多腿任务表单，全站保持浅色。
- 已完成：v2 ADL 监控闭环。管理员可按需刷新全部具名账户；Binance、OKX、Bybit、Bitget、Gate
  的原生等级统一保存为 1–5 级且 5 为最高风险，MEXC 保存明确的 private-event-only 能力记录而不
  伪造实时等级。页面展示账户、环境、合约、方向、五段风险条、原生值与观测时间。
- 已完成：参考项目的预警监控与邮件推送主页面接入真实通知 outbox。预警页按严重/警告聚合未处理、
  重试及 dead-letter 记录；邮件页只读展示 SMTP 安全模式、端口和凭据/收发件人是否已配置，不返回
  地址、用户名或密码，并可显式提交测试邮件、查看每次投递状态和脱敏错误码。
- 进行中：72 小时纸面运行、交易所沙盒循环和管理员明确触发的小额实盘验收。

## 1. 目标架构

将现有只读扫描器重构为单管理员、自托管的资金费率套利平台：

- 支持 Binance、OKX、MEXC、Bybit、Bitget、Gate。
- 策略支持跨交易所、同一基础资产的 2–16 条现货/USDT 线性永续腿；每个任务由一个主腿和
  1–15 个对冲腿组成。
- 支持纸面、交易所沙盒和真实账户三种环境。
- 支持手动配对下单，以及默认关闭、显式配置后才能启用的自动交易。
- Docker Compose 部署：Caddy、FastAPI、交易 worker、PostgreSQL。
- Web 与交易 worker 分离；所有交易指令先持久化，再由唯一 worker 执行。
- PostgreSQL 作为状态真相源，使用 Alembic 管理 schema；不迁移现有 SQLite 数据。
- 使用 PostgreSQL advisory lock 保证同一账户只有一个执行器，不引入 Redis。

## 2. 核心实现

### 交易所适配层

拆分公共行情与私有交易接口，统一提供：

- 标的、精度、合约乘数、最小名义金额、盘口、资金费和历史资金费。
- 余额、订单、成交、仓位、实际资金费收入及账户模式。
- 下单、撤单、查询、内部划转和私有 WebSocket。
- 客户端订单 ID、服务器时间校准、限流、签名、重连和 REST 对账。
- 自动检测单向/双向持仓模式；仅使用逐仓，杠杆默认 1 倍、最高 10 倍。
- 支持统一账户与传统账户差异；不自动切换存在持仓或挂单的账户模式。

实现以各交易所官方接口为准：

- [Binance](https://developers.binance.com/docs/binance-spot-api-docs/rest-api/trading-endpoints)
- [OKX](https://www.okx.com/docs-v5/en/)
- [Bybit](https://bybit-exchange.github.io/docs/v5/order/create-order)
- [Bitget](https://www.bitget.com/api-doc/spot/trade/Place-Order)
- [Gate](https://www.gate.com/docs/developers/apiv4/en/)

MEXC 使用已获特批的标准 V1 合约接口，但官方文档仍将合约下单标记为维护状态。
[MEXC 合约文档](https://mexcdevelop.github.io/apidocs/contract_v1_en/)
因此启动时必须执行能力探测；探测或小额验收失败时自动降级为只读监控，禁止伪装成可交易状态。

### 配对订单状态机

- 下单前验证数据新鲜度、机会状态、账户模式、余额、精度、额度、盘口容量、清算缓冲和私有连接状态。
- 固定 USDT 名义金额换算为共同基础币数量，按现货步长和合约乘数向下取整。
- 开仓使用并行保护价 IOC：现货买入价受最大滑点约束，永续做空价同样受保护。
- 下单前持久化交易意图、两腿客户端订单 ID 和配置版本；所有修改请求要求幂等键。
- 私有 WebSocket 确认成交，超时或断线时按客户端订单 ID 通过 REST 对账；不把单纯的下单 ACK 当作成交。
- 部分成交时只保留两腿共同数量，多余一腿立即发送保护性补偿单；无法中和时触发全局暂停并发送最高级告警。
- 平仓采用对称流程：卖出现货、以 reduce-only 买回永续，并按真实成交及手续费资产处理尾差。
- 重启后先同步余额、挂单、成交和仓位；完全一致才自动恢复原自动模式，否则保持暂停。
- “暂停”禁止新仓并撤销未成交订单，但保留已对冲仓位；“紧急清仓”独立存在并需要普通二次确认。
- 仅提供配对套利开平仓，不提供任意单腿或通用交易终端。

### 自动策略与风控

自动模式覆盖所有启用交易所的健康机会，不要求白名单，但必须显式配置以下参数：

- 单笔最大名义金额、单所与全局总敞口、最大并发仓位及单标的唯一仓位。
- 当前、24h、7d 最低资金费或年化、最低预计净收益、可接受基差。
- 最低两腿成交额、盘口容量倍数、正常与紧急最大滑点。
- 每日最大亏损、最短重入间隔、最长持有时间和最低清算缓冲。
- 自动平仓条件：资金费反转、收益跌破阈值、基差或盈亏保护、最长持有期及人工平仓。

默认自动模式关闭、所有实盘额度为 0；缺少任一必需阈值时拒绝启用。行情陈旧或交易所降级时禁止新仓，
但仍允许对账、补偿和平仓。

内部划转仅限同一交易所的现货/合约账户，受单次及每日限额约束，必须在下单前确认到账；永远不实现提现。

## 3. 安全、数据与接口

### 安全与账户

- 单管理员账户，Argon2id 密码、登录 TOTP、Secure/HttpOnly/SameSite Cookie、CSRF 防护和登录限流。
- 手动开平仓展示两腿价格、数量、费用、最坏滑点和余额影响，并做一次普通二次确认，不重复要求 TOTP。
- 交易所密钥使用环境主密钥进行 AES-256-GCM 加密，数据库保存密文、nonce 和密钥版本；API/UI 只返回掩码。
- API Key 只允许读取、交易和内部划转，禁止提现，并要求绑定 VPS 出口 IP。
- 日志、异常、通知和审计记录统一脱敏；所有配置、交易、划转、急停和登录动作写入不可修改的审计事件。

### 持久化模型

新增账户、凭据、策略版本、自动状态、交易意图、订单腿、成交、配对仓位、资金费收入、内部划转、
账户快照、告警、通知 outbox 和审计事件。金融数值使用 PostgreSQL `NUMERIC` 与 Python `Decimal`，
时间统一 UTC，交易所 ID 保存为字符串。

### HTTP/WebSocket 接口

- 认证：登录、TOTP、退出及会话状态。
- 账户：交易所账户增删改、掩码信息、连接测试和能力状态。
- 市场：机会、资金费历史、交易所状态。
- 交易：开仓预览/确认、平仓预览/确认、订单、成交、仓位及资金费收入。
- 自动化：配置、启用、暂停、恢复和紧急清仓。
- 运维：划转、通知测试、审计、健康状态和备份状态。
- 所有交易修改接口要求 UUID 幂等键；所有金融值仍以十进制字符串传输。
- WebSocket 改为认证连接，推送机会、订单状态、成交、仓位、风险和系统事件。

前端增加登录、账户、机会、订单预览、持仓、成交、资金费收益、自动策略、风险、通知、审计和系统健康页面；
实盘环境持续显示醒目的红色标识。

## 4. 通知与 VPS 运维

- Telegram 使用官方 [Bot API](https://core.telegram.org/bots/api)，允许 `/status`、`/positions`、
  `/alerts`、`/health` 等只读命令，按管理员 chat ID 白名单和 webhook secret 验证；
  禁止任何交易或配置命令。
- 邮件使用通用 SMTP STARTTLS/SMTPS。
- 普通机会与成交默认发 Telegram；失衡、失败、急停、账户异常和每日汇总同时发邮件与 Telegram。
- 通知使用数据库 outbox、去重键和指数退避；通知失败不得阻塞交易，通道故障由另一通道告警。
- Compose 仅公开 80/443，PostgreSQL 留在内部网络；Caddy 自动 TLS，容器非 root、只读文件系统、
  健康检查和自动重启。
- VPS 要求 SSH Key、主机防火墙、稳定时间同步和固定出口 IP；不实现地域限制绕过。
- 每日生成本地加密 PostgreSQL 备份，保留 7 个日备份和 4 个周备份，并提供恢复检查命令。
  仅 VPS 本地备份无法抵御整机丢失。
- 升级流程固定为：维护暂停、备份、数据库迁移、启动、对账、健康检查；失败时回滚镜像和数据库备份。

## 5. 验证、交付顺序与验收

按可独立验收的功能拆分提交并逐个推送：

1. PostgreSQL/Alembic、Docker Compose、Caddy、认证和密钥加密。
2. 重构公共行情层并加入 Bitget、Gate。
3. 六家账户只读接口、私有流和启动对账。
4. 纸面交易及持久化配对订单状态机。
5. 各交易所沙盒手动开平仓与内部划转。
6. 自动策略、组合平仓条件、风险限制、暂停和紧急清仓。
7. 邮件、Telegram、审计及完整前端。
8. VPS 运维、备份恢复、故障注入和用户人工触发的小额实盘验收。

测试覆盖：

- 六家签名、精度、合约乘数、仓位模式和错误映射。
- 重复请求、ACK 丢失、WebSocket 断线、部分成交、单腿失败、限流和时钟漂移。
- worker 在下单任意阶段重启后的幂等恢复。
- 额度、杠杆、每日亏损、陈旧数据和紧急补偿风控。
- 密钥加解密、日志脱敏、CSRF、会话和权限测试。
- Telegram 只读命令与 SMTP/Telegram 重试去重。
- 3,000 个候选标的负载、前端浏览器验证、PostgreSQL 备份恢复。
- 隔离容器验收会实际构建生产镜像并运行 PostgreSQL 17，覆盖迁移、模型/数据库漂移、
  API/数据库重启恢复、唯一 worker 锁、单轮 worker、认证加密备份、空库恢复和非空库拒绝覆盖；
  应用与备份容器使用只读根文件系统。
- 纸面模式连续运行 72 小时；支持沙盒的交易所完成重复开平仓循环。
- 最终实盘仅由管理员明确触发最小名义金额配对开仓并立即平仓；绝不进入 CI。
- MEXC 标准 V1 特批接口若未通过真实能力探测与小额验收，则保留监控、账户查询和告警，但禁用真实下单。

## 默认边界

- 当前运行时是单管理员；v2 任务支持同环境多个具名账户、跨所与任意 2–16 腿。旧私有流和旧同所
  交易器只消费交易默认账户，并仅保留在“系统管理”用于迁移期运维。
- 永续支持逐仓/跨仓建模，默认 1 倍且最高 10 倍；通用实盘执行器当前只放行已确认的逐仓，
  跨仓在逐所杠杆回读完成前失败关闭。
- 自动交易默认关闭，使用全新 PostgreSQL 数据库。
- Telegram 允许只读命令，不允许交易或配置命令。
- 不做跨交易所转账/提现、多租户或高可用集群；跨所策略要求各账户预先备资。

## 文档路由

| 任务 | 必读专题 |
|---|---|
| 交易所适配、符号归一化、收益计算、调度 | [行情与计算](docs/market-and-calculation.md) |
| HTTP/WebSocket、认证、前端交互 | [API 与界面](docs/api-and-ui.md) |
| PostgreSQL、配置、部署、验证、提交规范 | [存储与运维](docs/storage-and-operations.md) |

## 当前验证命令

```bash
.venv/bin/ruff check .
.venv/bin/pytest -q
.venv/bin/pip-audit -r requirements.lock
bash -n scripts/deploy_vps.sh
bash -n scripts/bootstrap_vps.sh
bash -n scripts/install_update_agent.sh
bash -n scripts/update_agent.sh
bash -n scripts/auto_update_agent.sh
pnpm --dir frontend test
pnpm --dir frontend build
docker compose --env-file .env.example config --quiet
.venv/bin/alembic heads
git diff --check
.venv/bin/python scripts/check_commit_messages.py --commit HEAD
```

Docker 可用时另运行不读取本地 `.env`、不接触真实账户的隔离容器验收：

```bash
.venv/bin/python scripts/container_acceptance.py
```
